import * as fabric from 'fabric';
const canvas = new fabric.Canvas(null, { width: 500, height: 500 });
const p = canvas.getScenePoint(new fabric.TPointerEvent( ... wait, I need a browser environment to run Fabric.
